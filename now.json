{
  "version": 2,
  "name": "saltinator",
  "builds": [
    {
      "src": "saltinator/package.json",
      "use": "@now/static-build",
      "config": {
        "distDir": "build"
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "saltinator/$1"
    }
  ]
}
